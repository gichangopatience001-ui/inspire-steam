var x = 20;
var y = 30;
// definition
function add_numbers (x,y){
    sum = x + y; 
    return sum;
}
// calling the function
console.log(add_numbers(x,y)):

// This function adds up all numbers from 1 to 50
function sumOfFifty() {
    var total = 0;

    for (var i = 1; i <= 50; i++) {
        total = total + i;
    }

    return total;
}

// Call the function and print the result
var result = sumOfFifty();
console.log("The sum of the first 50 numbers is: " + result);
```

